{
  "name": "clinical-notes-suite",
  "version": "1.0.0",
  "description": "Mobile-friendly Admission & Referral Note Templating System with Coupled Drug Sheets, SQLite, and docxtemplater",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "start": "cd server && node server.js",
    "build": "cd client && npm run build && cd ../server && npm install",
    "test": "cd server && node test.js"
  }
}
